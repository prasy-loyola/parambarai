import { RelationshipWithDetails } from './relationship-with-details';

describe('RelationshipWithDetails', () => {
  it('should create an instance', () => {
    expect(new RelationshipWithDetails()).toBeTruthy();
  });
});
